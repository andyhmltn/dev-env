---
description: Merge current worktree branch into main
---

Use the merge-worktree skill to merge this worktree's branch into main.

First, verify we're in a worktree and the working directory is clean. Then merge the branch into main and ask if I want to clean up the worktree afterward.
